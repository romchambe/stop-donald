<h1>Status <%= @game.status %></h1>
<table class="table table-hover" id='pending-game'>
	<tbody>
		<% @game.players.each do |player| %>		
			<tr>
				<td><%= player.username %></td>
			</tr>
		<% end %>
	</tbody>
</table>